<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Настройки хранения фотографий
    |--------------------------------------------------------------------------
    */

    // Базовый диск хранения
    'disk' => 'public',

    // Структура папок: properties/{property_id}/photos/
    'paths' => [
        'base' => 'properties',
        'photos' => 'photos',
        'originals' => 'originals',
        'thumbnails' => 'thumbnails',
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки загрузки
    |--------------------------------------------------------------------------
    */

    'upload' => [
        // Максимальное количество фото на объект
        'max_files' => 20,

        // Максимальный размер файла в килобайтах (10MB)
        'max_file_size' => 10240,

        // Разрешенные MIME типы
        'allowed_mimes' => [
            'image/jpeg',
            'image/png',
            'image/webp',
            'image/heic',
            'image/heif',
        ],

        // Разрешенные расширения
        'allowed_extensions' => ['jpg', 'jpeg', 'png', 'webp', 'heic', 'heif'],
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки обработки изображений
    |--------------------------------------------------------------------------
    */

    'processing' => [
        // Конвертировать HEIC в JPG
        'convert_heic' => true,

        // Формат для сохранения (после конвертации HEIC)
        'output_format' => 'jpg',

        // Качество JPEG (0-100)
        'quality' => 85,
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройки миниатюр (thumbnails)
    |--------------------------------------------------------------------------
    */

    'thumbnails' => [
        // Включить создание миниатюр
        'enabled' => true,

        // Ширина миниатюры в пикселях
        'width' => 300,

        // Высота миниатюры в пикселях
        'height' => 200,

        // Режим обрезки: 'fit' (вписать) или 'crop' (обрезать по центру)
        'mode' => 'fit',

        // Качество миниатюры
        'quality' => 80,
    ],

    /*
    |--------------------------------------------------------------------------
    | Формат имени файла
    |--------------------------------------------------------------------------
    | Доступные плейсхолдеры:
    | {original} - оригинальное имя файла (без расширения)
    | {timestamp} - Unix timestamp
    | {random} - случайная строка
    */

    'filename_format' => '{original}_{timestamp}',

];
